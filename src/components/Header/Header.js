import React from "react";

const Header = (props) => {
    return (
        <header className="App-header">
            <h2>Movie <span>4</span> evening</h2>
        </header>
    );
};

export default Header;